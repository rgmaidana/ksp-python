from setuptools import setup

setup(name='KSP',
      version='0.1',
      description='Python package with algorithms for finding the k-shortest-paths (e.g., 5 shortest) in a weighted directed graph.',
      url='http://www.github.com/rgmaidana/ksp-python',
      author='Renan Maidana',
      author_email='renan.g.maidana@ntnu.no',
      license='MIT',
      packages=['KSP'],
      install_requires=[],
      zip_safe=False
    )